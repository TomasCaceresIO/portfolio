---
import RetroLayout from './RetroLayout.astro';

interface Props {
  title?: string;
  description?: string;
  locationUrl?: string;
}

const props = Astro.props;
---

<RetroLayout {...props}>
  <slot />
</RetroLayout>
